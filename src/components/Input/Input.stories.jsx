import React from 'react';
import Input from './Input';

export const Primary = () => (
  <Input labelText='add new todo' placeHolder='todo' />
);

export default {
  title: 'Input',
  component: Input,
};
